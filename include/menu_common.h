/*#ifndef MENU_COMMON__INCLUDED
#define MENU_COMMON__INCLUDED

#include "tft.h"

class Coord {
    public:
        int x, y;
        Coord(int in_x, int in_y) {
            x = in_x;
            y = in_y;
        }
};

#ifndef MENU_MAX_PANELS
    #define MENU_MAX_PANELS 30
#endif

#endif*/